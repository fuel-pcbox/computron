asm
		[org 0x0]
		jmp _start
		hello db "Hello", 0x0d, 0x0a, 0x00
	_start:
		sti
		push cs
		pop ds
		mov si, hello
		xor bx, bx
	.loop:
		lodsb
		or al, 0x00
		jz .end
		mov ah, 0x0e
		int 0x10
		jmp .loop
	.end:
		jmp .end

_asm

result

_result
